import styled from '@emotion/styled';

export const ErrorMessage = styled.div`
  top: calc(100% + 20px);
  color: #dd4a48;
  border-radius: 6px;
  display: block;
  font-size: 11px;
  font-weight: bold;
  margin-top: 8px;
`;
